# OpenWrt x86_64 编译配置（适配 PVE/ESXi）
name: Build OpenWrt x86_64 for PVE/ESXi

# 触发条件：手动触发（推荐）+ 推送代码触发
on:
  workflow_dispatch:  # 手动触发（核心，避免自动编译消耗额度）
  push:
    branches: [ main ]
    paths:
      - '.github/workflows/openwrt-build.yml'

# 权限配置
permissions:
  contents: write

# 编译任务
jobs:
  build:
    runs-on: ubuntu-22.04  # 编译环境（和本地教程一致，最稳定）
    steps:
      # 步骤 1：检查代码
      - name: Checkout
        uses: actions/checkout@v4

      # 步骤 2：安装编译依赖（和本地教程一致，避免报错）
      - name: Install Dependencies
        run: |
          sudo apt update -y
          sudo apt full-upgrade -y
          sudo apt install -y ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential \
          bzip2 ccache cmake cpio curl device-tree-compiler fastjar flex gawk gettext gcc-multilib g++-multilib \
          git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libfuse-dev libglib2.0-dev libgmp3-dev \
          libltdl-dev libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libpython3-dev libreadline-dev \
          libssl-dev libtool lrzsz mkisofs msmtp ninja-build p7zip p7zip-full patch pkgconf python2.7 python3 \
          python3-pyelftools python3-setuptools qemu-utils rsync scons squashfs-tools subversion swig texinfo \
          uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev
          sudo apt --fix-broken install -y

      # 步骤 3：拉取 Lean 源码（稳定版）
      - name: Clone OpenWrt Source
        run: |
          git clone https://github.com/coolsnowwolf/lede.git openwrt
          cd openwrt
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      # 步骤 4：复制自定义配置文件（关键！替换为 x86_64 虚拟机配置）
      - name: Configure OpenWrt
        run: |
          cd openwrt
          # 生成适配 PVE/ESXi 的 x86_64 配置文件（核心）
          cat > .config <<EOF
          CONFIG_TARGET_x86=y
          CONFIG_TARGET_x86_64=y
          CONFIG_TARGET_x86_64_Generic=y
          # 生成 EFI 镜像（PVE/ESXi 必备）
          CONFIG_TARGET_IMAGES_GRUB_EFI=y
          CONFIG_TARGET_IMAGES_VMDK=y  # VMware/ESXi 专用格式
          CONFIG_TARGET_IMAGES_VDI=y    # 可选，VirtualBox 格式
          # 关闭不需要的镜像格式
          CONFIG_TARGET_IMAGES_ISO=y
          CONFIG_TARGET_IMAGES_VHD=n
          # 启用 LuCI Web 界面（必选）
          CONFIG_LUCI=y
          CONFIG_LUCI_LANG_zh_Hans=y
          CONFIG_LUCI_THEME_ARGON=y     # Argon 主题
          # 常用插件（按需增删）
          CONFIG_PACKAGE_luci-app-opkg=y
          CONFIG_PACKAGE_luci-app-sqm=y
          CONFIG_PACKAGE_luci-app-upnp=y
          CONFIG_PACKAGE_luci-app-ddns=y
          CONFIG_PACKAGE_luci-app-firewall=y
          CONFIG_PACKAGE_dnsmasq_full_dhcpv6=y
          CONFIG_PACKAGE_ipv6helper=y
          CONFIG_PACKAGE_kmod-usb3=y
          CONFIG_PACKAGE_kmod-usb-storage=y
          CONFIG_PACKAGE_kmod-usb-storage-extras=y
          CONFIG_PACKAGE_kmod-fs-ext4=y
          CONFIG_PACKAGE_kmod-fs-ntfs=y
          CONFIG_PACKAGE_luci-app-samba=y
          CONFIG_PACKAGE_luci-app-vsftpd=y
          CONFIG_PACKAGE_luci-app-frpc=y
          CONFIG_PACKAGE_luci-app-zerotier=y
          CONFIG_PACKAGE_luci-app-ddns=y
          CONFIG_PACKAGE_luci-app-wol=y
          CONFIG_PACKAGE_luci-app-upnp=y
          CONFIG_PACKAGE_luci-app-unblockmusic=y
          CONFIG_PACKAGE_luci-app-adguardhome=y
          CONFIG_PACKAGE_luci-app-accesscontrol=y
          CONFIG_PACKAGE_luci-app-arpbind=y
          CONFIG_PACKAGE_luci-app-autoreboot=y
          CONFIG_PACKAGE_luci-app-diskman_INCLUDE_btrfs_progs=y
          CONFIG_PACKAGE_luci-app-diskman_INCLUDE_lsblk=y
          CONFIG_PACKAGE_luci-app-filetransfer=y
          CONFIG_PACKAGE_luci-app-firewall=y
          CONFIG_PACKAGE_luci-app-nlbwmon=y
          CONFIG_PACKAGE_luci-app-ramfree=y
          CONFIG_PACKAGE_luci-app-rclone_INCLUDE_rclone-webui=y
          CONFIG_PACKAGE_luci-app-rclone_INCLUDE_rclone-ng=y
          CONFIG_PACKAGE_luci-app-ttyd=y
          CONFIG_PACKAGE_luci-app-turboacc=y
          CONFIG_PACKAGE_luci-app-turboacc_INCLUDE_OFFLOADING=y
          CONFIG_PACKAGE_luci-app-vlmcsd=y
          CONFIG_PACKAGE_luci-theme-bootstrap=y
          CONFIG_PACKAGE_luci-app-ssr-plus=y
          CONFIG_PACKAGE_luci-app-ssr-plus_INCLUDE_Xray=y
          CONFIG_PACKAGE_htop=y
          CONFIG_PACKAGE_automount=y
          CONFIG_PACKAGE_openssh-sftp-server=y
          CONFIG_PACKAGE_kmod-usb2=y
          CONFIG_PACKAGE_luci-app-samba4=y
          CONFIG_PACKAGE_luci-app-kodexplorer=y
          CONFIG_PACKAGE_luci-app-cifs-mount=y
          CONFIG_PACKAGE_luci-app-cifsd=y
          CONFIG_PACKAGE_luci-app-frps=y
          CONFIG_PACKAGE_luci-app-openvpn=y
          CONFIG_PACKAGE_luci-app-netdata=y
          CONFIG_PACKAGE_luci-app-v2ray-server=y
          CONFIG_PACKAGE_luci-app-adblock=y
          CONFIG_PACKAGE_luci-app-adbyby-plus=y
          CONFIG_PACKAGE_luci-app-airplay2=y
          CONFIG_PACKAGE_luci-app-aliyundrive-webdav=y
          CONFIG_PACKAGE_luci-app-docker=y
          CONFIG_PACKAGE_luci-app-noddos=y
          EOF
          # 加载配置并自动补全依赖
          make defconfig

      # 步骤 5：下载编译工具链（单线程，避免网络报错）
      - name: Download Tools
        run: |
          cd openwrt
          make download -j1 V=s

      # 步骤 6：开始编译（4线程，适配 GitHub 虚拟机）
      - name: Build OpenWrt
        run: |
          cd openwrt
          make -j4 V=s

      # 步骤 7：打包固件并上传到 Artifacts（方便下载）
      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: openwrt-x86_64-firmware
          path: openwrt/bin/targets/x86/64/

      # 步骤 8：（可选）上传固件到 GitHub Releases
      - name: Upload to Releases
        if: success()
        uses: softprops/action-gh-release@v2
        with:
          files: openwrt/bin/targets/x86/64/*.img.gz
          tag_name: build-${{ github.run_id }}
          name: OpenWrt x86_64 Build ${{ github.run_id }}
          body: "OpenWrt x86_64 firmware for PVE/ESXi (build ${{ github.run_id }})"
